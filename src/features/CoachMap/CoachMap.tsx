import classNames from "classnames";

interface CoachMapProps {
  bemClass?: string,
}

function CoachMap(props: CoachMapProps) {

  const className = 'coach-map'
  return (
    <div>
      
    </div>
  );
}

export default CoachMap;